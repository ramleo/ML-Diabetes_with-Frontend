# ML Pipeline Summary

_Generated by auto_pipeline.py on 2026-05-28 01:01:48_

---

## 1. Dataset Overview

| Property | Value |
|---|---|
| File | diabetes.csv |
| Rows | 768 |
| Columns | 9 |
| Target Column | `Outcome` |
| Task Type | **Classification** |
| Unique Target Values | 2 |
| Deployment Platform | render |

### Missing Values
No missing values detected.

---

## 2. Features Used

**Numeric (8):** `Pregnancies`, `Glucose`, `BloodPressure`, `SkinThickness`, `Insulin`, `BMI`, `DiabetesPedigreeFunction`, `Age`

**Categorical (0):** _none_

**Dropped (>50% missing):** _none_

---

## 3. Preprocessing Pipeline

| Step | Detail |
|---|---|
| Missing (numeric) | SimpleImputer(strategy="median") |
| Missing (categorical) | SimpleImputer(strategy="most_frequent") |
| Scaling | StandardScaler |
| Encoding | OneHotEncoder(handle_unknown="ignore") |
| Train/Test Split | 80/20, stratified, random_state=42 |
| Train size | 614 rows |
| Test size | 154 rows |

---

## 4. Model Selection & Hyperparameter Tuning

GridSearchCV(cv=3, n_jobs=-1, scoring="accuracy")

| Model | CV Score | Best Params |
|---|---|---|
| RandomForest | 0.7753 | {'max_depth': 5, 'n_estimators': 100} |
| LogisticRegression | 0.7688 | {'C': 0.1} |
| GradientBoosting | 0.7590 | {'learning_rate': 0.05, 'n_estimators': 100} |

**Winner:** `RandomForest` with CV accuracy = **0.7753**

---

## 5. Model Evaluation

| Metric | Value |
|---|---|
| Test Accuracy | 0.7273 |
| CV Score (accuracy) | 0.7753 |

**Classification Report:**
```
              precision    recall  f1-score   support

           0       0.76      0.85      0.80       100
           1       0.64      0.50      0.56        54

    accuracy                           0.73       154
   macro avg       0.70      0.68      0.68       154
weighted avg       0.72      0.73      0.72       154

```

---

## 6. Final Pipeline Architecture

```
CSV Input
    └── ColumnTransformer
            ├── numeric  → SimpleImputer(median) → StandardScaler
            └── categorical → SimpleImputer(most_frequent) → OneHotEncoder
                └── RandomForest(max_depth=5, n_estimators=100)
                        └── Prediction
```

---

## 7. Artifacts

| File | Description |
|---|---|
| models/final_pipeline.pkl | Trained sklearn Pipeline (preprocessor + model) |
| models/label_encoder.pkl | Fitted LabelEncoder for target classes |
| plots/eda_correlation.png | Feature correlation heatmap |
| plots/eda_target.png | Target variable distribution |


---

## 8. Reproducibility

```python
import joblib, pandas as pd

pipeline = joblib.load("models/final_pipeline.pkl")
df = pd.read_csv("data/diabetes.csv")
X = df.drop(columns=["Outcome"])
predictions = pipeline.predict(X)
print(predictions)
```
